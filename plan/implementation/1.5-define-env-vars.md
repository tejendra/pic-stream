# 1.5 Define environment variables – Implementation plan

## Summary

Define backend env vars JWT_SECRET, CRON_SECRET, GOOGLE_CLOUD_PROJECT, FIREBASE_STORAGE_BUCKET and document them in backend/.env.example with placeholder values. No secrets committed.

## Approach

- Add `backend/.env.example` listing all four variables with placeholder values and short comments.
- No code changes to read these yet (JWT_SECRET and CRON_SECRET are used in later work items). GOOGLE_CLOUD_PROJECT and FIREBASE_STORAGE_BUCKET are already read in config with defaults.
- .env and backend/.env remain in .gitignore so no secrets are committed.

## Files to add or change

| Path | Action |
|------|--------|
| `backend/.env.example` | Create (four variables, placeholders only) |

## Acceptance criteria

- [x] `backend/.env.example` lists those four variables
- [x] No secrets appear in the repo (`.env` and `backend/.env` are in `.gitignore`)
